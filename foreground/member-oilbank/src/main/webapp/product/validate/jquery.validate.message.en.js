/*
 * Translated default messages for the jQuery validation plugin.
 * Locale: CN
 */
jQuery.extend(jQuery.validator.messages, {
    required:"  mandatory informaiton",
    remote:"Error",
    email:"incorrect email, reenter",
    url:"incorrect URL, reenter",
    date:"incorrect date, reenter",
    dateISO:"incorrect date, reenter",
    number:"invalid number",
    digits:"0 &positive number only",
    creditcard:"incorrect credit card number, reenter",
    equalTo:"inputs different, check",
    accept:"unlegitimate suffix name",
    maxlength: jQuery.validator.format("maximum string lenth {0}!"),
    minlength: jQuery.validator.format("minimum string lenth {0}!"),
    rangelength: jQuery.validator.format("String length between {0}-{1}!"),
    range: jQuery.validator.format("input values between {0}-{1}!"),
    max: jQuery.validator.format("maximum input value {0}!"),
    min: jQuery.validator.format("minimum input value {0}!"),
    integer:"integers only",
    positive:"positive number only",
    positiveInteger:"positive integers only",
    mobile:"incorrect cellphone number, reenter",
    phone:"incorrect telephone number, reenter",
    zipCode:"incorrect zipcode, reenter",
    requiredTo:"mandatory information",
    username:"Chinese, English, number and underline only",
    prefix:"string begin with {0}! Only",
    lettersonly:"letters only"
});